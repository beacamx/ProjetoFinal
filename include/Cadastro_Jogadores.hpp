#include <iostream>
#include <vector>
#include "Jogo.hpp"
class cadastro{
  public:
  jogador * find(std::string)
  void cadastrar(jogador*);
  void remover(jogador*);
  private:
  vector<jogador*> jogadores;
}
